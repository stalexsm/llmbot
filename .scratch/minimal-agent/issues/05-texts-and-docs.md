# 05: Тексты и документация

**What to build:** Репозиторий честно описывает нового агента: текст /start рассказывает о возможностях (инструмент exec, скиллы, чат-сессия) и команде /new; README обновлён под агентную архитектуру (харнесс, цикл, скиллы, каталог данных сессий, новый порт исполнителя команд); пример конфигурации содержит все новые переменные окружения с комментариями. Свежий клон читается и запускается по README без сюрпризов.

**Blocked by:** 02, 03, 04 (описывает уже работающее поведение)

**Status:** ready-for-human

- [x] /start описывает агента, чат-сессию, /new и не обещает несуществующего
- [x] README отражает архитектуру агента и все слои, включая исполнителя команд
- [x] README документирует переменные окружения: лимит шагов, окно истории, таймаут инструмента, allowlist
- [x] Пример конфигурации содержит все новые переменные
- [x] Каталог данных чат-сессий упомянут и исключён из git
- [x] Все проверки (типы, линтер, форматирование, тесты) зелёные

## Comments

Реализация: `_START_TEXT` в `handlers.py` (агент, exec, скиллы, чат-сессия,
/new), README переписан под агентную архитектуру (диаграмма с AgentLoop,
ExecTool и ChatSessionStore; секции «Чат-сессии», «Скиллы», «Безопасность»;
таблица всех 13 переменных Settings), `env.example` дополнен всеми новыми
переменными с комментариями.

По итогам dual-ревью доки синхронизированы с кодом: `docs/architecture.md`
получил правила агентного слоя, исполнителя команд, скиллов и чат-сессий,
обновлённую цепочку композиционного корня и раздел ошибок (ToolResult вместо
исключений, SessionStorageError, честная остановка по лимиту);
`docs/runtime.md` — перечень агентных env-лимитов и каталога `.data/chats/`.

Попутный герметичный фикс: тесты `test_settings.py` читали локальный `.env`
через конструктор `Settings()` и падали при заполненном allowlist
(`Settings(_env_file=None)` теперь отключает env-файл). Четыре гейта зелёные.
