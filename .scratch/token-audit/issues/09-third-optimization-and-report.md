# 09: Третья оптимизация по данным аудита + отчёт до/после

**What to build:** по данным промежуточного аудита (bench с мониторингом) выбирается и внедряется третья оптимизация из списка: динамическое окно истории по токенам; чтение обычных файлов фрагментами (правка системного промпта); сжатие системного промпта. Скиллы не трогаются: индекс минимальный, полный файл читается по требованию. Затем — повторный bench-прогон после всех оптимизаций и итоговый отчёт до/после в docs: методика, цифры, guard качества. Тикет закрывает PR №2.

**Blocked by:** 06 (baseline), 07 (обрезка), 08 (компакция).

**Status:** done

- [x] Выбор третьей оптимизации обоснован данными промежуточного аудита и зафиксирован в отчёте
- [x] Повторный bench-прогон выполнен после всех трёх оптимизаций
- [x] Экономия ≥30% суммарных токенов (input+output) на запуск — или фактический результат зафиксирован с объяснением, почему выше не вышло
- [x] Guard качества: не больше одного дополнительного сбоя на наборе из 20 задач
- [x] Отчёт до/после опубликован в docs: методика, цифры, timeline примера запуска
- [x] Runtime-док дополнен всеми новыми настройками
- [x] Все четыре гейта зелёные

## Comments

- Ревью (две оси, параллельные саб-агенты): spec-ось нашла P2 — противоречие формулировок «скиллы не тронуты» при правке рендера секции индекса; исправлено: правились только обрамляющие формулировки вокруг списка, сам список (имя + первое предложение — усечение существовало до тикета), файлы скиллов и дизайн «индекс + чтение по требованию» не менялись. Standards-ось нашла P2-запах Duplicated Code — каноническая фраза «cat skills/<имя>/SKILL.md» в четырёх копиях; введена константа `SKILL_READ_COMMAND` в `skills.py`, её переиспользуют индекс, описание инструмента и подсказки самокоррекции exec (136/127/403).

- Выбор по данным промежуточного аудита (bench после 07+08, средний запуск 1534/403/1936): шаг 1 любого запуска стоил 831–849 input-токенов — это системный промпт плюс схема инструмента, повторяемый на каждом шаге; замеры частями через живую модель дали декомпозицию (схема ≈219, правила ≈240, дубль-секция `<tools>` ≈86, скиллы ≈165, дата ≈47). Кандидаты «окно истории по токенам» (в bench истории нет, рычаг не измерить) и «чтение файлов фрагментами» (выводы cat/ls после 07 съедают ~73 ток./запуск) отклонены как слабые; выбрано сжатие системного промпта.
- Сжатие затронуло и секцию индекса скиллов (`skills.py`): сокращены только обрамляющие формулировки вокруг списка; сам список и дизайн не тронуты — имя + первое предложение описания (усечение до первого предложения было в коде и до этого тикета), полный `SKILL.md` читается моделью по требованию.
- Финальный bench-прогон: success 20/20 (ноль доп. сбоев, гард пройден), средние 1167/561/**1728** — total −20.8% к baseline (2182), input −33.8%. Цель ≥30% по total не достигнута: output — поведение модели без безопасных рычагов и с разбросом ±150–200 ток./запуск; зафиксировано с объяснением в отчёте. Верных ответов 13/20 (как в промежуточном прогоне; падение с 16 случилось до сжатия промпта — шум малой выборки, в обе стороны).
- Отчёт: `docs/token-optimization-report.md` (методика, три прогона, per-task таблица, timeline bench-r01, обоснование выбора, воспроизведение). Runtime-док дополнен заметкой о сжатом промпте; новых env-настроек оптимизации не добавили (07 — прежний лимит, 08 — `AGENT_COMPACTION_KEEP_STEPS`, 09 — константы кода).
- Гейты: `uv run ty check`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest` (309 passed) — зелёные.
