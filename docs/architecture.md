# Архитектура: границы слоёв

Центральный принцип: `Telegram ≠ Application ≠ Agent ≠ Inference ≠ Ollama`.
Каждая граница изолирована и общается через типизированные контракты.

## Правила границ слоёв

- **Telegram-адаптер** (`src/bot/telegram/`): маппинг Telegram ↔ application DTO,
  вызов `ApplicationService`, обработка Telegram-ошибок. Этот слой не знает про
  Ollama ничего: ни URL, ни JSON, ни класса провайдера, ни логики инференса.
- **ApplicationService** (`src/bot/application/`): принимает типизированный вход,
  управляет чат-сессией (история + окно + блокировка на чат), вызывает
  агентный цикл, возвращает типизированный ответ. Не знает объекты
  Telegram API и JSON Ollama.
- **Агентный слой** (`src/bot/agent/`): `AgentLoop` крутит шаги «ответ модели →
  выполнение инструментов → следующий запрос» до финального ответа или лимита
  шагов. Инструменты — `Protocol Tool` (`bot.agent.tools`); цикл видит
  инструмент только через доменный `ToolSpec` (имя, описание, параметры).
- **Исполнитель команд** (`src/bot/agent/exec.py`): единственная реализация
  `Tool` — `ExecTool`: shell-команда в заданном рабочем каталоге (корень
  проекта), одна строка, таймаут с убийством группы процессов, обрезка
  вывода. Любое завершение — `ToolResult` (включая невалидные аргументы,
  таймаут, ненулевой exit code): сбой команды не исключение, а данные
  для следующего шага модели.
- **Скиллы** (`src/bot/agent/skills.py`): индекс `skills/<имя>/SKILL.md`
  собирается чистыми функциями в композиционном корне; полный текст скилла
  в промпт не попадает — модель читает файл сама через exec.
- **Чат-сессии** (`src/bot/sessions/`): `ChatSessionStore` — один JSONL-файл
  на чат (`.data/chats/<chat_id>.jsonl`), append-only. Системный промпт
  в хранилище не живёт: собирается заново при каждом запросе. Окно истории
  (`window.py`) берёт последние N сообщений так, чтобы пара «вызов →
  результат» не оказалась разорванной. Блокировка на чат живёт в
  `ApplicationService`: сообщения одного чата строго последовательны,
  разные чаты — параллельны.
- **InferenceProvider** (`src/bot/inference/provider.py`) — `typing.Protocol`;
  приложение зависит только от протокола. Новый провайдер добавляется без
  изменений application- и telegram-слоёв; замена Ollama не трогает их.
- **OllamaInferenceProvider** (`src/bot/inference/ollama.py`): HTTP-обмен,
  сериализация/парсинг, валидация ответа, таймауты, маппинг инфраструктурных
  ошибок на application-исключения. Без Telegram-логики. `httpx.AsyncClient`
  инжектится извне и переиспользуется всё время жизни — клиент на каждый запрос
  не создаётся.

## Состояние и внедрение зависимостей

- Глобальное изменяемое runtime-состояние на уровне модуля запрещено:
  `bot = Bot(...)`, `settings = Settings(...)`, провайдеры и сервисы как
  module-level инстансы. Разрешены определения — классы, функции, константы,
  `NewType`, `Protocol`.
- Зависимости внедряются явно, через конструкторы. Компонент создаёт свои
  инфраструктурные зависимости только снаружи — сам себе их не конструирует
  (`ApplicationService` не создаёт `OllamaInferenceProvider`).
- DI-контейнер (Dishka) не используется: сборка зависимостей ручная, в
  композиционном корне. Контейнер — отдельное будущее решение, и его ввод не
  должен требовать изменений domain/application-контрактов.
- Композиционный корень — `src/bot/main.py` (`run()`):
  Settings → httpx-клиент → OllamaInferenceProvider → ExecTool →
  индекс скиллов + системный промпт → AgentLoop → ChatSessionStore →
  ApplicationService → Bot/Dispatcher → polling. Владение жизненным циклом
  явное: HTTP-клиент и сессия бота закрываются в `finally`.

## Ошибки

- Инфраструктурные ошибки остаются вне application-слоя: адаптер Ollama
  маппит HTTP/сеть/таймауты на application-исключения (`InferenceError`,
  `InferenceTimeoutError`, `InferenceUnavailableError`).
- Telegram-слой превращает application-ошибки в безопасные сообщения.
  Пользователь получает raw-исключения, стеки, URL и детали инфраструктуры
  только через безопасный текст-заглушку — никогда напрямую.
- Сбои выполнения команды внутри цикла не распространяются наружу:
  исполнитель всегда возвращает `ToolResult`, модель читает результат
  (exit code, stdout, stderr) и решает, что делать дальше.
- Сбой хранилища сессии маппится в `SessionStorageError`; Telegram-слой
  показывает заглушку, сессия пользователя не портится (недописанный
  обмен просто не сохраняется).
- Исчерпание лимита шагов — не ошибка, а штатная остановка: `AgentRun`
  несёт флаг `stopped_by_limit`, пользователь получает честное сообщение
  об остановке (текст сообщения принадлежит Telegram-слою).

## Источник правил

Источник правил — `docs/homework1-specification.md` (полные формулировки,
примеры и обоснования — там). Спецификация — исходный исторический документ:
при эволюции кода правила фиксируются в `AGENTS.md`, справочниках `docs/` и ADR,
сам файл спеки не редактируется.
